#include "symplectic.hh"

#include <vector>
using std::vector;

vector<double> symplecticEulerStep(DynFun dynFun, double t, double dt,
                                   const vector<double> &params,
                                   const vector<double> &z) {
  vector<double> zRet(z.size());

  return zRet;
}